#include <stdio.h>
#include <string.h>

int main(){
    int N; scanf("%d", &N);
    while (N--){
        char den[12];
        int x, byk;
        scanf("%s", den);
        if (strcmp(den, "add")){
            scanf("%d %d", &x, &byk);
            while (byk--){
            }
        }
        
    }
    

    return 0;
}